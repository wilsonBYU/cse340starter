<% if (title) { %>
  <h1> <%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%- messages() %>

<form action="/account/login" method="post" id="loginForm">
  <label for="account_email">
    Email
    <input required type="email" name="account_email" id="account_email">
  </label>
  <label for="account_password">
    Password
    <input required type="password" name="account_password" id="account_password" pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$">
  </label>
  <input type="submit" value="LOGIN">
  <p>No account? <a href="/account/registration">Sign-up</a></p>
</form>